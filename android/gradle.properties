org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -XX:+UseG1GC -XX:MaxGCPauseMillis=2000 -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
android.compileSdkVersion=34
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
org.gradle.caching=true
org.gradle.workers.max=2
kotlin.daemon.jvm.options=-Xmx1024m
# Custom build directory removed to fix Flutter APK detection
# Set Gradle user home to avoid permission issues
org.gradle.user.home=C:\\Users\\SIYABONGA MKHIZE\\.gradle

# 16KB page size alignment for Android 15+ compatibility
# Handled in build.gradle through packagingOptions and splits configuration
# android.bundle.enableUncompressedNativeLibs removed (deprecated in AGP 8.1+)
android.injected.build.abi=arm64-v8a,armeabi-v7a

# CRITICAL FIX: Disable testOnly for release builds
android.injected.testOnly=false
